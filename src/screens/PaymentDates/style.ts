import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    text: {
        color: '#fff',
        fontSize: 15,
    },
    headerInfo: {
        color: '#fff',
        fontSize: 15,
        paddingBottom: 10,
        marginTop: -10,
    },
});

